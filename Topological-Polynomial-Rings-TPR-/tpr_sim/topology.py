class TopologicalStructure:\n    def __init__(self, coefficients):\n        self.coefficients = coefficients\n\n    def is_connected(self):\n        # Simple check: non-zero coefficients form a connected segment\n        non_zero = [i for i, a in enumerate(self.coefficients) if a != 0]\n        return len(non_zero) > 0 and max(non_zero) - min(non_zero) + 1 == len(non_zero)\n\n    def support(self):\n        # Indices of non-zero coefficients\n        return [i for i, a in enumerate(self.coefficients) if a != 0]\n
