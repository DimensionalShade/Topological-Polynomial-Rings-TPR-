from tpr_sim.semantics import SemanticCoefficient\n\ndef test_str():\n    s = SemanticCoefficient(5, 'legal')\n    assert str(s) == '5 [legal]'\n\ndef test_tags():\n    s1 = SemanticCoefficient(3, 'legal')\n    s2 = SemanticCoefficient(2, 'symbolic')\n    s3 = SemanticCoefficient(1)\n    assert s1.is_legal() == True\n    assert s2.is_symbolic() == True\n    assert s3.is_legal() == False\n
